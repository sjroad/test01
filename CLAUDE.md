# 산지로드 결산 자동화 대시보드 — 프로젝트 컨텍스트

이 문서는 Claude Code가 이 프로젝트를 다룰 때 알아야 할 배경 지식입니다.
코드만 봐서는 알 수 없는 "왜 이렇게 만들었는지"를 담았습니다.

## 프로젝트 목적

산지로드(식품 판매 업체)는 여러 온라인 판매 채널(카카오, 지마켓, 토스, 홈쇼핑 등)에서
상품을 팝니다. 채널마다 정산 방식(수수료율)이 달라서, 매일/매월 결산서를 정리하는 게
수작업으로는 번거로웠습니다. 이 프로그램은 "취합 결산서"(여러 담당자가 수기로 채운 원본
엑셀)를 넣으면, 사이트별 규칙에 따라 자동으로 정산가·마진·마진률을 계산해 "최종 결산서"를
만들어주고, 대시보드로 누적 데이터를 확인할 수 있게 해줍니다.

## 파일 구조

- `app.py` — Streamlit 웹 버전 (업로드/처리, 대시보드, 검색, 규칙관리 4개 탭)
- `app_desktop.py` — Tkinter 데스크톱(창) 버전. app.py와 동일한 로직(processing.py)을 사용하지만
  브라우저 없이 독립 창으로 뜸. PyInstaller로 exe 빌드 가능 (exe_만들기_윈도우.bat).
- `processing.py` — 핵심 계산 로직. 이 파일이 진짜 "엔진"입니다. 사이트별 규칙, 계산 함수,
  집계 함수가 모두 여기 있습니다.
- `data/site_rules.json` — 사이트별 수수료율 규칙 (키워드 매칭 방식)
- `data/cashdeal_exceptions.json` — 캐시딜 예외 상품 요율
- `data/fixed_price_table.csv` — 고정 정산단가 상품 목록 (아래 "특이 사이트" 참고)
- `data/settlement.db` — 누적 데이터 저장용 SQLite (로컬 파일, 커밋 대상 아님)

## 사이트별 정산 규칙 (왜 이렇게 되어 있는지)

원본 "취합 결산서"에는 판매사(사이트)별로 `결제금액` 컬럼이 있는데, 사이트마다 이 금액과
실제 `정산가`(우리가 받는 돈)의 관계가 다릅니다. 과거 담당자가 수기로 작성한 "최종 결산서"를
역산해서 아래 규칙을 찾아냈습니다 (site_rules.json에 반영됨):

- 카카오 90% (수수료 10%), 알리익스프레스 91%, 자사몰 96%, 네이버 스마트스토어 90%,
  티딜 85%, 토스 96.7%
- 지마켓/옥션/제이슨딜/GS SHOP/NS홈쇼핑/11번가 — 결제금액 = 정산가 (수수료 0%로 취급)
- 캐시딜 — 기본 90%, 단 "홍주부아카시아향사양꿀2.4kg" 상품만 80% (예외, cashdeal_exceptions.json)

## 토스 (주문마다 수수료가 다름) — 중요, 2026-09 추가

토스는 위 표의 고정 96.7%가 원래 근사치였고, 실제로는 **주문(상품)마다 수수료가 다릅니다**.
토스 셀러센터에서 받는 "주문배송관리" 엑셀의 F열(`받은 혜택`)에 힌트가 있습니다:

- F열에 `수수료 0원 적용` 문구가 있으면 → 수수료 3.3% (정산가 = 결제금액 × 0.967)
- F열이 빈칸이면 → 수수료 11% (정산가 = 결제금액 × 0.89)

그래서 `processing.py`에 `read_toss_delivery_file()`을 추가해 이 파일을 읽고
(`판매사주문번호` ↔ 배송관리 파일의 `주문번호`로 매칭), `process_settlement(..., toss_delivery_rates=...)`
로 넘기면 토스 주문 각각에 실제 요율이 적용됩니다. 이 파일을 안 넣으면 기존처럼
`site_rules.json`의 96.7% 정률로 fallback됩니다.

**주의**: 토스 "주문배송관리" 화면은 상태별(상품준비중/배송중/배송완료 등)로 필터링해서
다운로드하게 되어 있습니다. **이미 결산이 끝난 과거 날짜 주문을 매칭하려면 그 주문이 남아있는
상태(예: 전체 또는 배송완료)로 받아야 합니다** — "상품준비중" 상태로만 받으면 그 시점에 아직
처리 안 된 주문만 담겨 있어서, 이미 지난 결산 대상 주문과는 매칭률이 0%에 가까울 수 있습니다.
앱은 매칭 안 된 토스 주문을 "확인 필요" 목록에 넣어 조용히 틀리게 계산하지 않도록 해뒀습니다.

## 결산 대상 사이트 허용목록 (SETTLEMENT_SITES) — 2026-09 추가

취합 결산서에는 "맛장군", "쿠팡(개인/법인)", "테무", "농가살리기", "한경", "전라도청년",
"제철밥상" 같은 다른 유통 실험/위탁 채널 행이 섞여 들어오는데, **이건 산지로드 정식 결산
대상이 아닙니다.** `processing.py`의 `SETTLEMENT_SITES`에 정확한 사이트명 18개를
화이트리스트로 박아두고, `process_settlement()` 맨 앞에서 `판매사`가 이 목록에 **정확히
일치**하는 행만 계산하고 나머지는 `ProcessResult.excluded`로 따로 빼서 화면에 보여줍니다
(조용히 사라지지 않고 "제외됨" 건수/목록으로 확인 가능).

키워드가 아니라 "정확히 일치"로 비교하는 이유: "카카오"를 키워드로 하면 "맛장군 카카오"까지
걸려버리기 때문. 반대로 새 결산 대상 사이트가 생기면 `SETTLEMENT_SITES`에 **정확한 이름
그대로** 추가해야 합니다(오타/공백 하나만 달라도 조용히 제외 목록으로 빠집니다).

이 18개는 `REVIEWER_SITE_KEYWORDS`(박정배/김슬기/이재성)가 정확히 나눠 갖고 있어서,
`P.unassigned_sites()`는 정상 상황에서 항상 빈 리스트여야 합니다. 둘 중 하나만 고치고
나머지를 안 고치면 어긋나니 항상 같이 갱신하세요.

## 담당자별 원가 검토 워크플로 — 2026-09 추가

원가(매입단가)가 잘못 들어있는 경우가 있어서, 사이트 담당자가 "결산서 최종본"을 받아 자기
담당 사이트만 확인/수정한 뒤 다시 올리는 5단계 흐름을 만들었습니다 (`app.py`의 업로드 탭
순서와 동일):

1. 취합 결산서 업로드
2. 토스 주문배송관리 업로드 (선택)
3. 결산서 최종본 다운로드 (1차, 시스템 계산 그대로)
4. 담당자별 수정/검토 파일 업로드 — 박정배/김슬기/이재성 세 버튼. 3번에서 받은 파일을 담당자가
   열어 담당 사이트의 매입단가만 고친 뒤 그대로(형식 안 바꾸고) 재업로드
5. 결산서 최종본 다운로드 (2차, 담당자 검토가 반영된 진짜 최종본)

담당자별 소관 사이트는 `processing.py`의 `REVIEWER_SITE_KEYWORDS`에 있고, 판매사명에
키워드가 포함되면 매칭됩니다(대소문자 무시, site_rules.json과 같은 방식):

- 박정배: 더로드 네이버 스마트스토어, 더로드 자사몰, LG 복지몰, GS SHOP, 알리익스프레스,
  옥션, 제이슨, 지마켓, 카카오, 캐시딜
- 김슬기: NS홈쇼핑, 케이딜, 티딜, 홈앤쇼핑
- 이재성: 11번가, 꿈꾸는이웃, 토스, 제트언스

주의: "카카오"/"지마켓"/"토스" 등은 브랜드 접두어(맛장군/산지로드 등)와 무관하게 채널
자체로 배정했습니다. 반면 "더로드 네이버 스마트스토어"/"더로드 자사몰"은 일부러 "더로드"
브랜드로 한정했으므로, 예를 들어 "맛장군 네이버 스마트스토어"는 누구에게도 배정되지 않은
상태(`P.unassigned_sites()`로 확인 가능)입니다. 새 브랜드/채널이 생기면 이 딕셔너리를
갱신해야 합니다.

`apply_reviewer_corrections()`는 안전장치가 핵심입니다: 담당자가 올린 파일에 다른
담당자 사이트 행이 섞여 있어도(값이 잘못 들어있어도) 그 사이트가 keyword에 안 맞으면
**통째로 무시**합니다. 매칭은 (판매사, 고객선택옵션, 판매사주문번호) 조합 키로 하며,
매입단가만 반영하고 매입가/마진/마진률은 그 자리에서 다시 계산합니다(정산가는 건드리지
않음 — 수수료 로직이 아니라 원가 보정이 목적이므로).

### 4번 단계는 DB에 저장되는 "초안(draft)" 방식 — 2026-09 변경

처음엔 4번 단계(담당자별 검토)를 Streamlit `st.session_state`로만 구현했는데, 이러면
**같은 브라우저 세션 안에서 3명이 순서대로 올려야만** 동작합니다. 팀 공유(외부망+로그인)
환경에서는 담당자마다 각자 다른 컴퓨터/시간에 접속하므로 세션이 서로 분리되어 있어 이
방식이 안 통합니다. 그래서 `records`와 별개로 `drafts`/`drafts_excluded` 테이블(같은
DB, 같은 스키마)을 두고, 결산일자를 키로 "진행 중인 초안"을 저장합니다:

- **1번**(취합 결산서 업로드/처리) 직후, 그 날짜의 초안이 없으면 `save_draft()`로 자동
  저장합니다. 이미 진행 중인 초안이 있으면(=이미 누군가 검토를 시작함) 조용히 덮어쓰지
  않고, 사용자가 명시적으로 "초기화" 버튼을 눌러야만 담당자 검토 내용을 지우고 다시
  계산한 값으로 바꿉니다 — 실수로 담당자 작업을 날리는 걸 막기 위함입니다.
- **4번**은 이제 `result`(이 세션이 방금 처리한 데이터)가 아니라, `list_draft_dates()`로
  DB에 있는 모든 결산일자 목록을 보여주고, 사용자가 검토할 날짜를 고르면 `load_draft()`로
  그 시점까지의 최신 상태를 불러옵니다. 담당자가 파일을 올리면 `apply_reviewer_corrections()`
  적용 직후 바로 `save_draft()`로 다시 저장합니다 — **한 명이 언제 올리든, 다른 담당자가
  올렸는지 여부와 무관하게 즉시 반영되고 저장됩니다.**
- **5번**도 같은 방식으로 그 날짜의 최신 초안을 불러와 다운로드/누적 DB 저장에 씁니다.
- `drafts_excluded`는 결산서 최종본 엑셀에 맛장군/쿠팡 등 제외 사이트 행을 다시 포함시키기
  위한 것으로(원본 양식 유지), `result.excluded`를 그대로 같이 저장해둡니다.

이 초안 테이블은 "작업 중" 상태를 위한 것이라, 최종적으로 5번에서 "누적 DB에 저장"을 누르면
`records`(진짜 최종 누적 데이터)에 반영됩니다. 초안 자체를 지우는 기능은 아직 없습니다
(필요해지면 추가).

## 특이 사이트 (고정 정산단가 방식) — 중요, 자주 헷갈리는 부분

**케이딜, 꿈꾸는이웃, 홈앤쇼핑, LG복지몰, 제트언스** 이 5개 사이트는 위 방식이 안 통합니다:
- LG복지몰/제트언스는 취합 파일의 `결제금액` 컬럼 자체가 통째로 0으로 비어있음 (원본 데이터 결함)
- 홈앤쇼핑은 결제금액 값이 들어있긴 하지만 실제 정산 기준 금액과 다른 값(엉뚱한 금액)이 들어있음
- 케이딜/꿈꾸는이웃은 결제금액과 무관하게 **상품별로 정해진 고정 단가**로 정산됨 (같은 상품도
  시기에 따라 단가가 미세하게 다를 수 있음 — 완벽한 정답은 없고 "가장 흔한 값"을 시드로 넣어둠)

그래서 이 5개 사이트는 `fixed_price_table.csv`에 (판매사, 상품명, 개당 정산단가)로 등록해두고,
`정산가 = 정산단가 × 주문수량` 으로 계산합니다. 표에 없는 신규 상품이 나오면 "확인 필요" 목록에
뜨고, 사용자가 규칙관리 탭에서 직접 추가해야 합니다. **이건 자동화가 안 되는 영역이니 임의로
퍼센트 규칙으로 바꾸려고 하지 마세요.**

## 알려진 한계 (일부러 남겨둔 부분, "버그"가 아님)

1. 최종 결산서에서 담당자가 원가(매입단가)를 수기로 미세 보정하는 경우가 있어서, 이 프로그램의
   계산 결과와 과거 최종 파일의 "마진"이 정확히 일치하지 않을 수 있습니다 (정산가는 정확히 일치함
   — 이건 확인됨). 이건 원가 보정 문제지 수수료 로직 문제가 아닙니다.
2. `fixed_price_table.csv`의 시드 값은 특정 시점 최종 파일에서 추출한 값이라, 시간이 지나면
   정산단가가 바뀌어서 안 맞을 수 있습니다. 주기적으로 담당자가 확인/갱신해야 합니다.
3. Streamlit Cloud에 배포한 버전은 로컬 SQLite(`settlement.db`)를 씁니다. 클라우드 환경 특성상
   앱이 재시작되면 누적 데이터가 초기화될 수 있습니다 (알고 있는 제약, 아직 안 고침).

## 배포 흐름

이 프로젝트는 GitHub 저장소(`sjroad/test01`)에 연결되어 있고, Streamlit Community Cloud가
그 저장소의 `main` 브랜치를 보고 있습니다. **`main`에 push하면 자동으로 재배포**됩니다.
(현재 저장소는 임시로 Public 상태 — 원래는 Private이어야 하나 Streamlit Cloud 연동 권한
문제로 임시 조치했음. 나중에 Private로 되돌리고 권한을 제대로 설정하는 게 좋음.)

**주의**: 로컬 작업 폴더(`C:\Users\e23\Downloads\산지로드_결산_대시보드(zm)`)는 git 저장소가
아닙니다(`.git` 없음). 여기서 코드를 고쳤으면, 실제 배포에 반영하려면 사용자가 이 폴더의
파일들을 `sjroad/test01` 저장소에 직접 반영(커밋/푸시 또는 파일 업로드)해야 합니다.

## 팀 공유 & 로그인 (2026-09 추가) — 외부망 접속 + 로그인 방식 채택

사내 직원들과 공유하기로 하면서 "외부망에서도 접속 가능하되 로그인 필요" 방식을 선택했고,
기존 GitHub→Streamlit Cloud 자동배포 흐름을 그대로 유지하기로 했습니다. 필요한 설정은
**코드가 아니라 Streamlit Cloud 대시보드 설정**입니다:

1. share.streamlit.io에서 이 앱의 **Settings → Sharing**으로 들어가서 "Who can view this app"을
   Private으로 바꾸고, 접근을 허용할 팀원 이메일을 등록합니다. 이러면 등록된 이메일로 로그인해야만
   접속되고, 외부에서도(재택 포함) 그대로 접속됩니다. 추가 코드는 필요 없습니다.
2. 팀원을 추가/제거할 때는 이 이메일 목록만 관리하면 됩니다 — `app.py`를 고칠 필요 없음.

이 방식의 전제 조건이 "누적 데이터 지속성"이라, 아래 DB 이전을 반드시 같이 해야 합니다.

## 누적 데이터 저장소 — 로컬 SQLite → 클라우드 DB (2026-09 추가)

Streamlit Community Cloud는 앱이 재배포/재시작될 때마다 컨테이너 디스크를 초기화합니다.
그동안 `data/settlement.db`(로컬 SQLite 파일)에 누적 데이터를 저장해왔는데, 클라우드에
배포된 채로 팀이 계속 쓰면 재시작될 때마다 데이터가 날아갑니다. 그래서 `app.py`의 DB 계층을
SQLAlchemy 기반으로 바꿔서, **Streamlit secrets에 `[connections.sql]` 설정이 있으면 그
DB(예: Supabase Postgres)를 쓰고, 없으면 지금까지 쓰던 로컬 SQLite 파일로 자동 대체**하도록
했습니다 (`app.py`의 `get_engine()`). 로컬에서 `streamlit run app.py`로 그냥 켜서 개발할 때는
지금처럼 SQLite로 동작하니 신경 쓸 게 없고, **배포 환경에서만** 아래처럼 secrets를 채워주면
됩니다:

1. Supabase(https://supabase.com)에 무료 프로젝트를 하나 만든다 (또는 다른 Postgres 호스팅).
2. 프로젝트의 Settings → Database에서 연결 문자열(Connection string, URI 형식)을 복사한다.
3. Streamlit Cloud의 이 앱 → Settings → Secrets에 아래처럼 넣는다(TOML 형식):
   ```toml
   [connections.sql]
   url = "postgresql://postgres:[비밀번호]@[호스트]:5432/postgres"
   ```
4. 저장하면 앱이 재시작되면서 그 Postgres DB를 쓰기 시작합니다. **기존에 로컬 SQLite에 쌓인
   데이터는 자동으로 옮겨지지 않으니**, 옮기고 싶으면 `data/settlement.db`를 읽어서 새 DB에
   `to_sql`로 부어주는 1회성 마이그레이션 스크립트가 필요합니다(아직 안 만들었음).
5. `app_desktop.py`(오프라인 개인용 프로그램 버전)는 이 클라우드 DB로 안 바꿨습니다 — 원래
   목적 자체가 "브라우저/네트워크 없이 로컬에서 혼자 쓰는 버전"이라 계속 로컬 SQLite를
   씁니다. 팀 공유용 데이터 누적은 웹 버전(app.py) 기준입니다.

`requirements.txt`에 `sqlalchemy`, `psycopg2-binary`가 추가되었습니다(Postgres 드라이버).

새로 만든 `.gitignore`가 `data/settlement.db`와 `.streamlit/secrets.toml`(DB 비밀번호가 든
파일)을 커밋 대상에서 제외합니다 — 이전에는 `.gitignore`가 아예 없었어서, 특히 지금처럼
저장소가 Public인 상태에서는 이 두 파일이 실수로 올라가면 안 됩니다. **secrets.toml은 절대
git에 커밋하지 말고 Streamlit Cloud의 Secrets 설정에만 입력하세요.**

## 앞으로 개선하면 좋은 것 (사용자와 논의된 사항)

1. ~~누적 데이터 저장을 로컬 SQLite에서 클라우드 DB로 옮기기~~ — 코드는 완료(위 참고),
   실제 Supabase 계정 생성 및 Streamlit Cloud secrets 등록은 사용자가 해야 함.
2. `fixed_price_table.csv`를 매달 새 최종 결산서를 넣으면 자동으로 갱신/학습하는 기능.
3. GitHub 저장소를 Private으로 전환하고 Streamlit Cloud 연동 권한을 제대로 재설정
   (현재 Public이라 secrets/DB 비밀번호를 코드에 절대 하드코딩하면 안 됨 — 위 .gitignore 참고).
4. 기존 로컬 `data/settlement.db`에 쌓인 데이터를 새 클라우드 DB로 옮기는 1회성 마이그레이션
   스크립트.

## 코드 수정 시 주의사항

- `processing.py`의 `DEFAULT_RATE_RULES`, `DEFAULT_CASHDEAL_EXCEPTIONS`는 "기본값"이고,
  실제 운영 중에는 `data/*.json`, `data/*.csv` 파일이 우선 사용됩니다 (앱에서 수정하면 이 파일들이
  갱신됨). 기본값을 고치는 것과 실제 운영 데이터를 고치는 것을 헷갈리지 마세요.
- `app.py`(Streamlit)와 `app_desktop.py`(Tkinter)는 별도 UI지만 같은 `processing.py`를
  공유합니다. 계산 로직을 고칠 때는 반드시 `processing.py`만 고치고, 두 UI 모두에서 잘
  동작하는지 염두에 두세요.
